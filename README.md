# GIS_utils

###To install:
Clone the repository, and then from a command prompt in the repository folder:
```python
$python setup.py install
```

###To import:
```python
import GISio
import GISops
```
